# IT-Dart — Projekt-Status

Kompakte Orientierung für jede neue Sitzung. Details: GO_LIVE.md, COMPLIANCE.md, `todos`-Tabelle, Claude-Gedächtnis. Regel zur Pflege: siehe CLAUDE.md „Projekt-Status-Datei".

**Fertig:** Kernplattform (8 Module, Prüfungsvorbereitung, KI-Chat, PDF-Lernnachweis) · AGB/Datenschutz/Impressum/Consent · Icon-Set + Hero-Bild + finalisierter Partikeleffekt auf der Unternehmensseite · KI-Kosten-Dashboard, Tool-Register, To-Do-Board mit KI-Lösungsvorschlag · Unit-Tests für PDF-Generatoren · Technischer Sicherheitsaudit vollständig abgeschlossen (search_path gehärtet, 2 verwaiste Edge-Function-Duplikate gelöscht, Leaked-Password-Protection aktiv, Supabase Pro-Upgrade) · E2E-Testsuite läuft grün gegen einen wegwerfbaren Supabase-Sandbox-Branch (8/8, deckt jetzt auch den Modul-Render-Pfad ab) · Musterantworten-Rollout (a1/a2, kein Live-API-Aufruf bei Klick auf eine Musterfrage) über alle 8 Module abgeschlossen (#63/#77) · `ITDart.jsx`-Refactor (#89) abgeschlossen: von 1528 auf ~330 Zeilen, 6 neue eigenständige Dateien (Scene/Logo/ModuleUi/Quiz/AIChat/moduleContent.js) · E2E-Backlog #97/#98/#99/#100/#101/#102/#104 abgeschlossen: echte Trainer-Kontingent-/Einladungs-, KI-Dialog- (Interview/Diagnose), Premium-PDF-, Onboarding- und Konto-Löschungs-Tests, 16/16 grün · #103 (Admin-Bereich) abgeschlossen: neue E2E-Rolle E (Voll-Admin-Testkonto), AdminScreen.jsx-Nutzerverwaltung + alle 6 Unterwerkzeuge getestet, 18/18 grün · #105 (Registrierung/Reset/Magic-Link) abgeschlossen ohne echten E-Mail-Versand (generateLink() statt SMTP), 21/21 grün — kompletter E2E-Backlog aus #97–#105 damit erledigt bis auf den einen offenen App-Bug #108.

**Offen:** Stripe-Einrichtung (#43, Finom-Support-Antwort zu wiederkehrenden Zahlungen steht noch aus) · Amazon-Partnernet für Affiliate-Tools-Leiste (#70) · zwei Scrollbalken auf Mobilgeräten gemeldet, noch nicht reproduziert (Screenshot ausständig, evtl. durch Scroll-to-top-Fix erledigt) · IT-Dart-Kids Bildgenerierung + Anwaltsprüfung · Troubleshooter noch nicht live · Logo-Redesign pausiert. **Versicherung ist erledigt** — IT-Haftpflicht am 2026-07-31 unterschrieben (andsafe/Finanzchef24), gültig ab **2026-08-03**; Wartungsmodus bleibt bis dahin bewusst aktiv, siehe #54. Alle harten GO_LIVE.md-Punkte sind damit erledigt oder terminiert. Von #74 ("Vor Go Live"-Check) sind der E-Mail-Webserver-Check (notify.it-dart.de vollständig verifiziert, siehe unten) sowie E2E-Lauf und Testkonten-Aufräumen erledigt (2026-08-02: E2E-Suite auf wegwerfbaren Sandbox-Branch umgestellt statt Produktivdatenbank, 6/6 grün; die 4 alten dauerhaften Produktiv-Testkonten aus dem älteren `setup-test-users.mjs`-System samt zugehöriger 8 GitHub-Secrets gelöscht, dieses System bleibt als dokumentierter manueller Fallback bestehen, Entscheidung über Behalten/Entfernen in 30 Tagen) — nur noch der Gesamtcheck offen. DPMA-Markenklassen-Frage für IT-Dart-Kids bleibt separat offen (liegt beim Anwalt, nicht blockierend, betrifft nicht die Haupt-Plattform). Neuer Notfall-Gesamtwiederherstellungsplan (`dokumentation/30_Notfall_Gesamtwiederherstellungsplan.docx`) hat noch mehrere „AUSFÜLLEN"-Platzhalter (Fundorte für Passwort-Manager/Registrar-Zugang/2FA-Codes, Versicherungs-Kontaktdaten, DNS-Soll-Werte) — bis der Nutzer diese ergänzt, ist der Plan nur teilweise einsatzbereit.

**Zuletzt geändert (2026-08-05, Teil 25): Claude-Zugriffsmodell für die Supabase-MCP-Verbindung vereinbart.** Ausgangsfrage des Nutzers ("2FA überall aktivieren, damit sich mein Zugriff auf Anfragen beschränkt") ehrlich korrigiert: 2FA schützt nur den interaktiven Login, nicht MCP-/Token-Zugriff. Stattdessen echte Konfigurationshebel recherchiert und vereinbart: Supabase-MCP-Server unterstützt `project_ref` (auf ein Projekt beschränken, deaktiviert kontoweite `account`-Werkzeuge), `read_only=true` (technisch keine Schreiboperation möglich) und `features=<gruppen>` (Positivliste erlaubter Werkzeuggruppen). Vereinbartes Modell: Standardzustand `project_ref=vukqjpqawzhfvpjjpipp&read_only=true&features=database,debugging,docs`, Erweiterung auf Schreibzugriff nur auf konkrete Anfrage für einen Auftrag, danach zurücksetzen — praktisch pro Sitzung, nicht pro Nachricht (Verbindung lässt sich nicht mitten im Gespräch umschalten). Dokumentiert in CLAUDE.md (neuer Abschnitt "Claude-Zugriffsmodell") und doc 30 (neuer Abschnitt 4.4, Verweis statt Duplikat). GitHub-Token-Zugriff bewusst als separat zu bewertender, noch offener Punkt vermerkt. Noch nicht umgesetzt: die eigentliche Umkonfiguration der MCP-Verbindung selbst muss der Nutzer in seinen Connector-Einstellungen vornehmen, das kann ich nicht selbst.

**Zuletzt geändert (2026-08-05, Teil 24): echte Supabase-Rechtsnachweise gesichert + neuer Unterauftragsverarbeiter-Fund.** Nutzer hat über das Supabase-Dashboard ("Legal Documents") das DPA (Version 1, 01.08.2026) und die Transfer Impact Assessment als PDF heruntergeladen, als `dokumentation/42_Nachweis_Supabase_DPA.pdf`/`43_Nachweis_Supabase_TIA.pdf` gespeichert (verifiziert: 396KB/857KB, echter Inhalt per PDF-Preview geprüft, keine korrupten Downloads). Dabei ein echter neuer Fund in der TIA: Supabase setzt selbst weitere Unterauftragsverarbeiter ein (AWS für DB-Hosting, Google Cloud/BigQuery für Logs, Fly.io für Realtime, Cloudflare + Upstash) — bisher nirgends in doc 09 erwähnt, jetzt in Abschnitt 6 nachgetragen. Beide PDFs in doc 09 (Bekannte-Lücken-Abschnitt + Verarbeitungsverzeichnis-Tabelle) als Beleg verlinkt. SOC2/ISO 27001/Security-Questionnaire bewusst nicht angefragt (brauchen Team-Plan-Upgrade). Nutzer prüft als Nächstes die entsprechende Vercel-Seite.

**Zuletzt geändert (2026-08-05, Teil 23): 4 von 6 AVV-Nachweisen real geklärt.** Per WebFetch gegen die aktuellen Rechtstexte verifiziert (nicht angenommen): Vercel, Anthropic, Resend und Supabase binden ihr Auftragsverarbeitungs-DPA automatisch durch Bezugnahme in die Standard-ToS ein ("incorporated by reference"/"becomes legally binding upon entering into the Agreement") — mit Kontoeröffnung bereits gültig. STRATO verweist in AGB §1.4 auf eine separate AVV-Seite, vermutlich ebenfalls automatisch einbezogen, aber weniger explizit belegt. **GitHub bleibt offen** — aus öffentlichen Dokumenten nicht klärbar, ob das DPA für ein einzelnes Free/Pro-Konto (kein Enterprise-Agreement) automatisch gilt. doc 09 (Verarbeitungsverzeichnis-Tabelle + Bekannte-Lücken-Abschnitt + Abschnitt 6) entsprechend aktualisiert.

**Zuletzt geändert (2026-08-05, Teil 22): doc 09 zum Datenschutz-Prüfungs-Vorlagedokument ausgebaut + Wöchentlich-Korrektur + Knowledge-Base-Pass.** Auf Nutzerwunsch: (1) Pflege-Rhythmus überall von "alle 4 Wochen" auf "wöchentlich" verschärft (COMPLIANCE.md, CLAUDE.md, doc 09). (2) `IT-Dart-Project-Knowledge.md` (Nutzer hat sie zwischenzeitlich vervollständigt) mit 24 Statusmarkierungen (ENTSCHEIDUNG/UMGESETZT/GEPLANT/IDEE-OPTION) gegen den echten Code-Stand korrigiert, keine strukturelle Migration (§70 docs/-Ordner) durchgeführt wie angewiesen. (3) `dokumentation/09_Datenschutz_Referenzdokument.docx` zum echten Prüfungs-Vorlagedokument ausgebaut statt ein neues Dokument anzulegen (eine Quelle der Wahrheit): neues Verarbeitungsverzeichnis nach Art. 30 DSGVO (10 Verarbeitungstätigkeiten, konsolidiert aus Abschnitt 5/6) direkt nach der Kopfzeile eingefügt, plus konsolidierte "Bekannte Lücken"-Übersicht (6 Punkte, aus den bestehenden ❓-Markierungen + COMPLIANCE.md + doc 29 zusammengeführt) -- bewusst transparent, damit bei einer echten Prüfung nichts überrascht. Externe Datenschutzerklärung (`LegalPages.jsx`) bleibt unverändert die einzige nach außen sichtbare Fassung.

**Zuletzt geändert (2026-08-05, Teil 21): IT-Dart-Compliance als echte Funktion aufgebaut.** Auf Nutzerwunsch, ausgelöst durch die Frage „wie stellen wir sicher, dass Richtlinien eingehalten werden". Fünf Teile: (1) COMPLIANCE.md aktualisiert — zwei echte, gerade gefundene veraltete Stellen korrigiert (Minderjährigen-Status war noch als "nicht nachgerüstet" markiert, obwohl seit 2026-08-04 gebaut-aber-inaktiv; AGB-Consent-Gate-Fix für Einladungslink-Nutzer nachgetragen), neuer Score 19/21. (2) **Neues Admin-Aktions-Protokoll** (`admin_action_log`, Migration `20260805040000_admin_action_log.sql`) — DB-Trigger auf `profiles` erfasst automatisch JEDE Änderung an is_admin/is_premium/is_trainer/is_junior_admin/trainee_limit/ai_enabled sowie Kontolöschungen, unabhängig vom Schreibpfad; schließt eine in CLAUDE.md seit Längerem benannte echte Lücke (kein Protokoll, wer wann Rechte vergeben hat). Bekannte Grenze: Akteur nur bekannt bei Sitzungs-authentifizierten Änderungen, nicht bei service-seitigen Edge-Function-Vorgängen. Per echtem Test-Toggle verifiziert (Trigger feuert korrekt, RPC-Join liefert korrekte E-Mails) — zwei harmlose Test-Einträge bleiben im Log stehen, da DELETE auf die Audit-Tabelle selbst von der Auto-Mode-Sperre blockiert wurde (passender Nebeneffekt: die Tabelle ist dadurch auch für mich selbst nicht trivial nachträglich manipulierbar). (3) Neues `AuditLogScreen.jsx`, admin-only über "📜 Audit-Log" erreichbar. (4) CLAUDE.md ergänzt: Lückenbefund als behoben markiert, neue Regel für periodischen Gegencheck (mind. alle 4 Wochen, nicht nur ereignisgetrieben) statt nur bei offensichtlich passender neuer Funktion. (5) Neuer Abschnitt 16 in `dokumentation/09_Datenschutz_Referenzdokument.docx`: Coşkun Bulut als dokumentierter Compliance-Verantwortlicher, mit Begründung warum aktuell keine gesetzliche DPO-Pflicht besteht (Art. 37 DSGVO/§38 BDSG, Schwelle ~20 Personen) und expliziten Triggern für eine Neubewertung.

**Zuletzt geändert (2026-08-05, Teil 20): neues Branch-Kosten-Tracking — nach einem echten Sicherheitseinwand des Nutzers komplett umgebaut, jetzt Push statt Poll.** Ursprünglicher Entwurf (Edge Function pollt alle 15 Min. die Supabase-Management-API) wurde verworfen: der Nutzer wies zu Recht darauf hin, dass ein Management-API-Personal-Access-Token hochkritisch ist — gegen die echte Supabase-Doku verifiziert ("PATs carry the same privileges as your user account"), klassische PATs haben **keine Scopes und kein eingebautes Ablaufdatum**, Least-Privilege ist mit diesem Tokentyp technisch gar nicht umsetzbar. Token-Erstellung gestoppt, Feature neu entworfen: `.github/workflows/e2e-tests.yml` meldet Branch-Start/-Ende jetzt direkt per Shared-Secret-Push an die Edge Function `branch-cost-sync` (gleiches, bereits bewährtes Muster wie `e2e-report-ingest`: `verify_jwt:false`, `timingSafeEqual`-Vergleich, neues GitHub+Edge-Function-Secret `BRANCH_COST_SECRET`) — keine Management-API mehr nötig. `pg_cron`-Job umgebaut von "poll+HTTP-Call" zu einem reinen SQL-Sicherheitsnetz (`branch-cost-safety-net`, schließt nur Zeilen, die älter als 3h und fälschlich noch "active" sind, kein externer Aufruf mehr). Tabelle `branch_cost_log` + RPC `get_branch_cost_summary()` unverändert gültig. Neues Panel "Supabase-Branch-Kosten" in `CostDashboardScreen.jsx`. **Bewusste Lücke:** ad-hoc per Supabase-MCP angelegte Debug-Branches (wie der aus Teil 19) werden NICHT automatisch erfasst — geringeres Risiko wichtiger als lückenlose Automatik. Alter, bereits live gewesener Poll-Cron-Job wurde live deaktiviert; eine Korrektur-Migration (`20260805030000_branch_cost_cron_fix.sql`) räumt das bei jedem künftigen Branch-Replay automatisch mit auf (Korrektur des historischen Migrations-Eintrags selbst war durch die Auto-Mode-Sperre blockiert, vom Nutzer bewusst auf „bei Gelegenheit" statt sofort erzwungen vertagt). **Erledigt und verifiziert (2026-08-05):** `BRANCH_COST_SECRET` gesetzt (GitHub per `gh secret set`, Supabase manuell durch den Nutzer) — per echtem Test-POST gegen die live deployte Funktion bestätigt (Status 200, Zeile kam korrekt in `branch_cost_log` an, danach wieder gelöscht). Der zuvor erzeugte, nicht mehr benötigte Management-API-Token wurde vom Nutzer gelöscht. Nächster echter End-to-End-Beweis folgt beim nächsten echten E2E-Lauf (Start-/Ende-Meldung durch den Workflow selbst statt eines manuellen Tests).

**Zuletzt geändert (2026-08-05, Teil 19): echter Migrationsfehler in `profiles_display_ref` (Teil 18-Nachgang) gefunden und behoben.** Zwei E2E-Läufe in Folge scheiterten beim Branch-Aufbau ("username_welcome_seen"-Spalte fehlte) — zunächst fälschlich als bekannte Bootstrap-Timing-Verzögerung behandelt (Retry-Budget 24s→60s erhöht, half nichts). Echte Ursache per eigens angelegtem Debug-Branch isoliert: `select setval(..., coalesce(max(display_ref),0) ...)` scheitert mit "value 0 is out of bounds", sobald `profiles` leer ist — auf jedem frischen Branch der Fall (Branches kopieren nie echte Daten), lief auf der Produktion nur zufällig durch, weil dort bereits 24 Zeilen existieren. Gespeicherte Migrationszeile in `supabase_migrations.schema_migrations` direkt korrigiert (`greatest(...,1)` + 3-Parameter-`setval` mit `is_called`-Flag), NICHT nur die lokale Datei — sonst hätte der nächste Branch dieselbe alte, fehlerhafte Version erneut abgespielt. Fix durch einen echten, vollständigen E2E-Lauf verifiziert (Run 30962142370, `e2e_runs.id=63`): 22/22 grün auf einem komplett neuen Branch. Ein Wegwerf-Debug-Branch (`debug-username-schema`, Projekt `pgpvtkwtofllxjwedzcs`) muss noch manuell im Supabase-Dashboard gelöscht werden (Anleitung im Chat gegeben, Löschung selbst war durch die Auto-Mode-Berechtigungssperre blockiert).

**Zuletzt geändert (2026-08-04, Teil 7):** #108 interaktiv per Browser-Debugging weiterverfolgt (diesmal erfolgreich, nachdem Teil 6 an fehlendem Service-Role-Key-Zugriff scheiterte — Nutzer hat den Key sicher direkt im Supabase-Dashboard für eine neue Wegwerf-Branch kopiert, nie im Chat). Zwei echte Funde: (1) neues, unabhängiges Problem — `@supabase/gotrue-js` verwirft jede URL-Session, deren Token laut `iat`-Claim älter als 120s ist, ohne dass die App das abfängt (To-Do #111, betrifft jeden echten Nutzer, der eine E-Mail nicht sofort öffnet); (2) wahrscheinliche Ursache von #108 selbst — bei einem frischen Link rendert ResetPasswordScreen korrekt, aber die Session wird zwischen Aufbau und Formular-Abschicken aktiv wieder entfernt (localStorage leer, `getSession()` liefert nichts mehr), vermutlich durch den Session-Kickout-Mechanismus (Realtime-Kanal reagiert fälschlich auf die eigene `claim_session()`-Änderung). Exakter Trigger noch nicht 100% isoliert, aber die Richtung für einen Fix ist jetzt klar (Heartbeat/Kickout-Effect während `recoveryMode` nicht mounten) — Details in To-Do #108. Debug-Infrastruktur (Branch, `.env.debug108.local`/`.env.branch108.local`, temporäres `launch.json`-Profil, temporäre Konsolen-Logs) vollständig wieder entfernt, `git diff` auf den betroffenen Dateien leer. Neue Standard-Regel im Gedächtnis verankert: Secrets nie in den Chat einfügen, stattdessen lokale gitignored Datei (`feedback_secrets_never_in_chat`).

**Zuletzt geändert (2026-08-04, Teil 18): Compliance-Lücke tatsächlich geschlossen, nicht nur dokumentiert.** Neuer AgbConsentScreen.jsx (analog needs_password_setup) zeigt per Einladungslink registrierten Nutzern nach dem ersten Login denselben, bereits geprüften Einwilligungs-Checkbox-Wortlaut -- sofort live, kein Feature-Flag (keine neue Rechtstext-Formulierung, nur ein nachgeholter Zustimmungsschritt). Migration diesmal sofort mitanwendet (nicht nur committet), Lehre aus dem Produktionsfehler von heute Morgen (Teil 15) beachtet. Zusätzlich: Datenschutzerklärung nennt jetzt den Claude-Code-Datenbankzugriff, AGB §11 hat jetzt denselben Streitbeilegungshinweis wie das Impressum (beide doc-29-Punkte als erledigt markiert). E2E-Blast-Radius war größer als gedacht: seed-e2e-users.js musste zentral angepasst werden (agb_consent_given=true für alle Testkonten), sonst hätte JEDER Test in JEDER Rollendatei den neuen Gate-Screen getroffen. Vollständig verifiziert: kompletter E2E-Lauf (alle 6 Rollen) 22/22 grün gegen echten Sandbox-Branch.

**Zuletzt geändert (2026-08-04, Teil 17):** Neues Dokument 41 (Rechtstext: Registrierungsformular) erstellt -- vollständiger Formular-Kontext (Felder, Meldungen, Ablauf, Abgrenzung zur Einladungslink-Registrierung) ergänzend zu doc 40 (nur der Checkbox-Wortlaut). In doc 29 Tabelle 4 und doc 32 Anlagen verdrahtet. Anwalts-Paket jetzt 6 eigenständige Rechtstext-/Kontext-Dokumente (36-41) statt der ursprünglich kombinierten doc 35.

**Zuletzt geändert (2026-08-04, Teil 16): echte Compliance-Lücke gefunden.** Die Einwilligungs-Checkbox (AGB + Datenschutz) existiert ausschließlich in AuthScreen.jsx mode="register" -- per Magic Link eingeladene Nutzer (Trainer-/Admin-Einladung) durchlaufen dieses Formular nie (EinladungScreen.jsx navigiert direkt zum Link, Nutzer landet bereits authentifiziert). Für diesen vermutlich überwiegenden Teil der real angelegten Konten existiert damit aktuell KEIN expliziter Zustimmungsmechanismus zu AGB/Datenschutzerklärung. Als neuer Punkt in doc 29 Abschnitt 6 für den Anwalt festgehalten ("Fehlender Zustimmungsmechanismus bei Einladungslink-Registrierung"), doc 40 entsprechend korrigiert (enthielt zuvor die falsche Behauptung, beide Wege seien identisch abgesichert). Noch keine Code-Änderung, nur Dokumentation -- Umsetzung eines Zustimmungs-Screens für Einladungslink-Nutzer wartet auf anwaltliche Einordnung.

**Zuletzt geändert (2026-08-04, Teil 15): echter Produktionsfehler behoben.** Die in Teil 12 gebaute (inaktive) Altersverifikation hatte `AuthContext.jsx`s Profil-Query um `birthdate, parent_consent_confirmed` erweitert und das wurde live gepusht, OHNE die zugehörige Migration `20260804000000_minor_consent.sql` anzuwenden — die Spalten fehlten in der echten Datenbank, jede Profil-Abfrage schlug seitdem fehl und fiel auf ein Default-Profil mit `is_admin/is_trainer/is_premium: false` zurück. Betraf ALLE eingeloggten Nutzer, nicht nur einen — der Nutzer meldete es als "bin nicht mehr Admin". In der Datenbank war `is_admin` nie verändert, rein clientseitiges Symptom. Migration direkt per Supabase-MCP nachgeholt (`apply_migration`), Fix live verifiziert (echte Ziel-Query gegen die DB liefert jetzt korrekt `is_admin:true`). Lehre in Claude-Gedächtnis (`feedback_migration_deploy_ordering`) festgehalten: Migrationen künftig sofort mitanwenden, sobald bereits gepushter Code von den neuen Spalten abhängt, nicht als reinen "bitte manuell nachholen"-Hinweis behandeln.

**Zuletzt geändert (2026-08-04, Teil 14):** Auf Nutzerwunsch ("jede Vereinbarung sollte ihre eigene Vereinbarung haben") die kombinierte doc 35 in 5 eigenständige Dokumente aufgeteilt: 36 (Impressum), 37 (Datenschutzerklärung), 38 (AGB), 39 (Trainer-Vereinbarung), 40 (Einwilligungs-Checkbox — bisher komplett gefehlt, da nie als eigener Rechtstext erfasst). Doc 29/32 verweisen jetzt überall darauf. Doc 35 ist damit überflüssig, aber bewusst nicht selbst gelöscht. Dabei zwei echte Zirkelverweise in doc 29 gefunden und behoben ("siehe Punkt 4" innerhalb von Abschnitt 4 selbst geschrieben) sowie einen Selbstwiderspruch (Cookies-Absatz als "keine offene Frage" übertitelt, endete aber mit einer echten Bitte). Nutzer hatte zuvor zurecht kritisiert, dass eine frühere Zusammenfassung eine Korrektur als erledigt gemeldet hatte, die tatsächlich noch nicht gemacht war — Lehre in Claude-Gedächtnis (`feedback_thoroughness_and_precision`) festgehalten.

**Zuletzt geändert (2026-08-04, Teil 13):** Echter, vom Nutzer gemeldeter Bug behoben: ein bereits angemeldeter Nutzer wurde durch Klick auf "Über IT-Dart" effektiv ausgeloggt. Ursache doppelt: (1) `App.jsx` zeigte WartungScreen.jsx bei `page==="company"` unabhängig vom Login-Status; (2) `ITDart.jsx` zeigte view "auth" rein aus dem URL-Parameter `?mode=login`, ebenfalls unabhängig davon — ein zweiter erfolgreicher Login-Versuch dort hätte per Single-Session-Enforcement die eigene, noch aktive Sitzung hinausgeworfen. Beide Stellen prüfen jetzt zusätzlich `!user`. Neuer E2E-Regressionstest in `roleF.auth-flows.spec.js`, live gegen einen echten Sandbox-Branch verifiziert (4/4 grün, Run 30940343875, nach einem Locator-Fix im Test selbst — `CompanyScreen.jsx` rendert den "Zum Lerntool"-Button zweimal). Alles aus Teil 12 sowie dieser Fix committet und nach `main` gepusht (2 Commits, Vercel deployed automatisch).

**Zuletzt geändert (2026-08-04, Teil 12):** Doc 29 um 5 weitere Anwalts-Prüfpunkte ergänzt (Streitbeilegung/VSBG und Auftragsverarbeiter-Liste dabei als bereits live erledigt korrigiert, waren fälschlich noch als offen markiert; Änderungsklausel/salvatorische Klausel/zwingender Haftungs-Carve-out neu). Live umgesetzt (sicher, kein Anwalts-Vorbehalt nötig): AGB §7 Haftung um Körper-/Leben-/Gesundheit-Pflichtausschluss + KI-Inhalte-Disclaimer erweitert, §11 salvatorische Klausel entfernt (§ 306 BGB, im B2C wirkungslos) — noch NICHT in `dokumentation/35_...` nachgezogen (Datei war beim Nutzer offen, Sync pausiert auf Nutzerwunsch). Neu gebaut, aber komplett inaktiv (`MINOR_CONSENT_ENABLED=false` in `src/lib/featureFlags.js`): Geburtsdatum-Erfassung + Eltern-E-Mail-Doppel-Opt-In für unter-16-Jährige bei Selbstregistrierung — Migration (`profiles.birthdate/parent_email/parent_consent_confirmed/parent_consent_token`), neue Edge Function `parent-consent` (submit/resend/confirm), neue Screens `BirthdateSetupScreen.jsx`+`ParentConsentConfirmScreen.jsx`, Gate in `App.jsx` nach demselben Muster wie `needs_password_setup`. Migration + Edge Function brauchen noch das übliche manuelle Deployment, bevor irgendetwas davon wirkt. Neuer Unternehmens-Slogan „Gezielt ans Ziel." ergänzt in `CompanyScreen.jsx` (aktuell wegen WARTUNGSMODUS nicht live erreichbar) und `WartungScreen.jsx` (live verifiziert) — bewusst nicht im Produktkontext ("Bleib am Dart!") und noch nicht im Impressum, um keinen weiteren doc-35-Sync auszulösen.

**Zuletzt geändert (2026-08-04, Teil 11):** Auf Nutzerwunsch STRATO/GitHub ebenfalls in die live Datenschutzerklärung aufgenommen (eigener, klar abgegrenzter Ergänzungssatz nach der Hauptdienstleister-Liste, da andere Rolle als die aktiv ausgewählten Kernanbieter). Doc 29 um Amazon-KDP-Markenfrage fürs künftige IT-Dart-Kids-Malbuch ergänzt sowie einen neuen Abschnitt, der klarstellt, dass E-Books (laufen über Amazons eigenes Vertragswerk) und 3D-Druck-Produkte (aktuell kein eigener Checkout) die zu prüfenden Rechtstexte nicht berühren. Alle Code-/Doku-Änderungen dieser Session noch NICHT committet/gepusht — bewusst zurückgehalten, bis der Nutzer entscheidet, ob er das vor oder nach dem Anwaltsgespräch live stellen möchte.

**Zuletzt geändert (2026-08-04, Teil 10):** Anwalts-Vorbereitung um 10 vom Nutzer eingebrachte Punkte erweitert, nach echter Code-/Recherche-Prüfung (nicht geraten): IT-Dart-Kids bewusst NICHT in den Prüfauftrag aufgenommen (nur beiläufig im Anschreiben erwähnt, Doc 32), da ein Festpreis nur für bereits fertige Texte sinnvoll ist. Admin-Direkteinladung in Doc 29 ergänzt (bisher nur Trainer-Weg dokumentiert). Echter Live-Gap gefunden und behoben: `Resend` fehlte tatsächlich in der öffentlichen Datenschutzerklärung (`src/LegalPages.jsx`) — To-Do #86 war zwar als "erledigt" markiert, aber nur die internen Docs (29/05) waren aktualisiert, nicht der Live-Text. STRATO/GitHub bewusst nicht in den öffentlichen Text (keine Verarbeitung registrierter Nutzerdaten). Stripe+Finom-Recherche: technisch mit Steuernummer-Platzhalter möglich, aber bewusst nicht genutzt — Go-Live bleibt an die echte Steuernummer gekoppelt. Neues Dokument 34 (Aufbewahrungsfristen-Übersicht) erstellt: DSGVO-Tabellen (alle Cascade-Delete bei Kontolöschung, Speicherort Supabase EU-West-2/London), externe Dienstleister, sowie Abgrenzung zu den gesetzlichen 8-10-Jahres-Aufbewahrungspflichten fürs Steuerrecht (GoBD/HGB, separates Thema für den Steuerberater). Lernnachweis-Speicherung geprüft: PDF nie serverseitig gespeichert, nur ein Metadaten-Protokoll. Neues To-Do #113 (Freunde-laden-Freunde) als spätere Idee erfasst, bewusst nicht Teil dieses Prüfauftrags.

**Zuletzt geändert (2026-08-04, Teil 9):** #111 behoben (verständliche deutsche Meldung statt rohem "Auth session missing!" bei zu alten/bereits verwendeten Links) — reine Ergänzung in `src/lib/errorText.js`, per neuem Unit-Test abgesichert statt eines CI-Branch-Laufs. Damit ist auch dieser letzte offene Punkt aus der #108-Untersuchung geschlossen.

**Zuletzt geändert (2026-08-04, Teil 8):** #108 vollständig behoben und verifiziert (3/3 grün, kein Retry mehr nötig). Drei echte Ursachen über mehrere CI-Diagnoserunden mit gezieltem Konsolen-Logging gefunden: (1) `updatePassword()` setzte `recoveryMode` zu früh auf `false`, ein dadurch auf der alten Seite kurz vor dem automatischen Reload abgeschickter `claim_session`-Request erreichte den Server trotz Client-seitigem Verbindungsabbruch noch und überschrieb als verspätetes Echo den sauberen Neu-Claim der frisch geladenen Seite; (2) `type=recovery` hatte anders als `type=invite` keine synchrone Ausgangserkennung, das asynchrone `PASSWORD_RECOVERY`-Event kam manchmal zu spät; (3) ein reiner Testfehler (zu frühe Navigation nach "Abmelden", festes statt zufälliges Test-Passwort). Alle drei in `src/lib/AuthContext.jsx` bzw. `e2e/specs/roleF.auth-flows.spec.js` behoben, temporäre Diagnose-Logs wieder restlos entfernt. Damit ist der komplette E2E-Backlog aus #97–#108 abgeschlossen. Verbleibend aus dieser Untersuchung: #111 (eigenständiger, noch offener Fund — `supabase-js` verwirft Links, die älter als 120s sind, ohne verständliche Fehlermeldung).

**Zuletzt geändert (2026-08-04, Teil 6):** `todos`-Tabelle aufgeräumt: 14 tatsächlich abgeschlossene Einträge (u. a. #45 Ökosystem-Bewertung, #54 Versicherung, #59 Logo-Entscheidung, #88 E-Mail-Templates final, #97–#105 kompletter E2E-Backlog) explizit auf `done` gesetzt — Einträge mit echtem offenen Rest (#76, #78, #91, #6, #106/#107/#109/#110) bewusst nicht angerührt. Zwei neue Musterschreiben erstellt und an den Nutzer übergeben: `dokumentation/32_Anschreiben_Anwaltsanfrage.docx` und `33_Anschreiben_Steuerberateranfrage.docx`, jeweils zum Verschicken zusammen mit den bereits bestehenden Briefing-Dokumenten (29 bzw. 28) an mehrere Kanzleien/Steuerberater, inkl. recherchierter Kandidaten-Empfehlungen. #108-Debugging versucht, aber an einer echten Tooling-Grenze gescheitert: der Service-Role-Key eines Supabase-Branches ist nur innerhalb des GitHub-Actions-Laufs verfügbar (über ein dort hinterlegtes Secret), lokal/über die Supabase-MCP-Verbindung nicht abrufbar — ein eigens dafür angelegter Debug-Branch wurde deshalb wieder gelöscht, ohne dass echtes interaktives Debugging stattfinden konnte. #108 bleibt offen, bräuchte entweder einen weiteren CI-Lauf mit temporärer Diagnose-Ausgabe oder Zugriff auf den `SUPABASE_ACCESS_TOKEN`. Nebenbei einen eigenen Fehler gefunden und bereinigt: ein fehlgeschlagener `cp`-Befehl mit Windows-Backslash-Pfaden hatte ein leeres Fantasie-Verzeichnis im Projekt-Root angelegt (Git-Bash-Escaping-Problem) — folgenlos entfernt.

**Zuletzt geändert (2026-08-04, Teil 5):** Echte Supabase-Warnung wegen hoher Bounce-Rate erhalten (Projekt `wlgsicpwovccwuxqiaut` — kein Produktivprojekt, sondern einer der vielen wegwerfbaren E2E-Branches von heute, bereits automatisch gelöscht). Ursache: der `roleF`-Passwort-Reset-Test rief `resetPasswordForEmail()` live gegen eine garantiert nicht existierende Adresse auf, obwohl die App-Erfolgsmeldung ohnehin nie verrät, ob die Adresse existiert — der echte Zustellversuch war für die Testaussage nie nötig, jetzt per `page.route()` gemockt. Zusätzlich `roleE`/`roleF` als eigene, einzeln wählbare Suiten ergänzt (inkl. `e2e-trigger-run`-Redeploy über die Supabase-MCP-Verbindung), damit künftiges gezieltes Debugging einer einzelnen Rolle nicht mehr zwangsläufig auch `roleC`s echten Einladungs-Versand mit auslöst. `roleC`s echter Versand selbst bleibt bewusst live (echte Kontingent-Trigger-Prüfung, Nutzerentscheid). Verifiziert per gezieltem `roleF`-Lauf (3/3, Run 55, kein `roleC`-Versand ausgelöst), getaggt `e2e-bounce-fix-2026-08-04`. Unbestätigte Vermutung als To-Do #110 festgehalten: E2E-Branches erben möglicherweise nicht das konfigurierte eigene SMTP der Produktion und fallen auf Supabases geteilten Standard-Mailer zurück.

**Zuletzt geändert (2026-08-04, Teil 4):** #105 (Registrierung/Passwort-Reset/Magic-Link) umgesetzt — kein echter E-Mail-Versand/-Empfang nötig, `generateLink()` (Supabase Admin-API) erzeugt exakt denselben Link, den sonst die E-Mail enthalten hätte, direkt im Test unmittelbar vor Gebrauch generiert (nicht im Seed-Skript — erste Version scheiterte, weil die Links dort schon abgelaufen waren, bis `roleF` dran war). Diagnose-getriebener Weg mit mehreren echten Zwischenfunden: (1) Signups sind projektweit deaktiviert (von Produktion geerbt) — der allgemeine Registrierungs-Fallback-Link in `AdminScreen.jsx` führt für echte Nutzer aktuell ins Leere, als To-Do #107 festgehalten, auf Nutzerwunsch nicht behoben; (2) `redirectTo` ohne abschließenden Slash wurde von `generateLink()` still verworfen und fiel auf die Produktions-Site-URL zurück (echter Bug in meinem Testcode, jetzt mit Slash behoben); (3) Recovery-/Invite-Link-Klicks lösen bei `updateUser()` zuverlässig "Auth session missing!" aus — ursprünglich als kurzes Timing-Race vermutet, über mehrere Läufe widerlegt (auch 12s+ Retry ändert nichts), Ursache noch offen (To-Do #108), braucht interaktives Browser-Debugging. Signup-Link-Test läuft vollständig durch, die zwei betroffenen Tests sind mit `test.fail()` markiert statt die Suite rot zu halten. Endstand: 21/21 grün (Run 54), getaggt `e2e-auth-flows-2026-08-04`. Damit ist der komplette E2E-Backlog (#97–#105) abgearbeitet.

**Zuletzt geändert (2026-08-04, Teil 3):** #103 (Admin-Bereich) umgesetzt. Seed-Skript: neues `admin`-Testkonto (`is_admin:true`) + rein passives `adminTarget`-Konto als Mutations-/Löschziel (nie eines der vier bestehenden Rollen-Konten dafür verwenden, sonst Race gegen deren eigene parallele Tests) — keine neuen GitHub-Secrets nötig, Zugangsdaten laufen wie bei den anderen Rollen über `GITHUB_ENV`, frisch pro Lauf generiert. Neue `roleE.admin.spec.js`: volle Nutzerverwaltung (Premium/Trainer/Junior-Admin vergeben+entziehen, Löschen eines AKTIVEN Kontos — admin-exklusiv, Junior-Admins dürfen laut Rolle D nur ausstehende Einladungen löschen) sowie ein Rendering-Smoke-Test für alle 6 admin-exklusiven Unterwerkzeuge (E2E-Tests/Website-Check/Kosten/Monitoring/To-Do/Feedback), bewusst ohne Klick auf echte Schreib-/Kosten-Aktionen (kein "Testlauf starten", das würde einen rekursiven E2E-Lauf auslösen). Ein echter Strict-Mode-Locator-Bug unterwegs gefunden und behoben (Badge-Text war Substring des zugehörigen "...entfernen"-Buttons). `build-report.js` kannte "Rolle E" noch nicht — ergänzt, sonst wären die neuen Tests im Dashboard unsichtbar mitgelaufen. Endstand: 18/18 grün (Run 47), getaggt `e2e-admin-role-2026-08-04`. Verbleibend: #105 (Registrierung/Reset/Magic-Link, braucht echten E-Mail-Empfang).

**Zuletzt geändert (2026-08-04, Teil 2):** E2E-Backlog aus Teil 1 abgearbeitet (7 von 9 Punkten: #97–#102, #104). Seed-Skript: `trainee`-Testkonto jetzt `is_premium`, `trainer`-Testkonto `trainee_limit:2`. Neue echte Tests: Trainer-Kontingent+Einladung in einem Test (spart 3 E-Mails/Lauf), Bewerbungshilfe-KI (Mock-Interview) + Fehleranalysesystem (Diagnose-Dialog) mit echten API-Aufrufen, voller Premium-Quiz + echter PDF-Download (deterministisch über GQ-Text-Abgleich statt Zufalls-Klicks — "immer erste Option" träfe nur ~25%), Onboarding-Feedback, Konto-Löschung, Statistik. Dabei zwei echte Zwischen-Bugs gefunden und behoben (Timeout zu knapp, Strict-Mode-Locator) sowie eine bisher unsichtbare Infrastruktur-Lücke aufgedeckt: die Branch-Bereitschaftsprüfung im Workflow deckt nur die DB/API-Schicht ab, nicht Edge-Function-Secret-Propagation — über 3 Läufe reproduziert (auch nach ~96s Retry kein Erfolg, also nicht zeitabhängig), als To-Do #106 mit `test.fail()` sauber markiert statt die Suite rot zu halten. Dabei auch einen echten Bug in `build-report.js` selbst gefunden und behoben (zählte `test.fail()`-Fälle fälschlich als Fehler, obwohl Playwright sie korrekt als bestanden wertet). Endstand: 16/16 grün (Run 45), getaggt `e2e-backlog-2026-08-04`. Verbleibend als eigenes Vorhaben: #103 (Admin-Bereich, braucht neues privilegiertes Testkonto) und #105 (Registrierung/Reset/Magic-Link, braucht echten E-Mail-Empfang).

**Zuletzt geändert (2026-08-04, Teil 1):** `ITDart.jsx`-Refactor (#89) in 5 einzeln verifizierten, getaggten Speicherpunkten umgesetzt (`refactor/checkpoint-1`…`-5`, jeweils per E2E gegen den Feature-Branch abgesichert), dann konfliktfrei nach `main` gemerged und live deployed (verifiziert: 0 Konsolenfehler, App-Bundle lädt sauber). Lerninhalte → `src/lib/moduleContent.js`, `Scene`+Hilfskomponenten → `src/Scene.jsx`, `Logo` → `src/Logo.jsx` (5 externe Importer umgestellt), `OSIOverview`/`Pips`/`Hdr`/`OSIBezug` → `src/ModuleUi.jsx`, `Quiz`/`AIChat` → eigene Dateien. Die 4 verbleibenden Inline-Views (locked/cover/overview/mod) bleiben bewusst in `ITDart.jsx` — Props-Drilling-Risiko wurde gegen den bereits erreichten Zeilen-Nutzen abgewogen, Nutzer hat "hier aufhören" gewählt. Dabei eine echte E2E-Lücke gefunden und geschlossen: keine der vier Rollen-Specs öffnete je ein Modul — zwei neue Tests in `roleA.free.spec.js` decken jetzt Scene/Hdr/Pips/AIChat-Rendering + Quiz-Abschluss + Freemium-Sperre ab (bewusst ohne echten KI-Aufruf). Neun weitere E2E-Lücken als To-Dos #97–#105 erfasst (PDF-Erfolgsfall, Admin-Bereich, Trainer-Kontingent, KI-Dialogmodi, Registrierung, …), nicht Teil dieser Änderung.

**Zuletzt geändert (2026-08-03):** Bitwarden als Passwort-Manager eingerichtet (Konto, Mobile-App, Erweiterung, Chrome-Import) und 2FA-Rundgang durch GitHub/Vercel/Supabase/STRATO/Resend/Gmail abgeschlossen (Anthropic-Konsole/OpenArt haben bewusst kein eigenes 2FA, Schutz hängt am E-Mail-Postfach) — Rest (GPG-Backup-Passphrase-Rotation, Doku-Abschluss von Dokument 30) bleibt To-Do #91. Dokument 29 (Anwalts-Paket) dreifach erweitert: fehlende Auftragsverarbeiter (STRATO/Resend/GitHub) samt Vorschlagstexten, fehlende Abo-/Widerrufsregelungen (§312k/§355/§309 BGB) für die künftige Stripe-Anbindung, und die bisher komplett fehlende Trainer-Vereinbarung als fünfter zu prüfender Rechtstext ergänzt; Dokument 05 (Sicherheitskonzept) dabei bereinigt (veraltete Discord-Zeile entfernt, dieselben drei Dienstleister nachgetragen). Neues Dokument 31 (Investitionsplan und Steuerhandhabung): Investitions-Tracking-Tabelle, Marketing-Budget-Phasenplan, mehrere steuerliche Detailfragen für den Steuerberater — dabei zwei echte Fehleinschätzungen aus einem ChatGPT-Chat des Nutzers per echten Projektdaten korrigiert (die vermeintlichen „708 € API-Kosten" sind Claude-Code-Entwicklungsnutzung, nicht ein Fehler im Plattform-KI-Chat, der real nur ca. 0,04 € kostete; die CAC/LTV-Beispielrechnung war auf einen falschen Premium-Preis von 12 €/Monat statt der echten 3,99 € gestützt). Kleiner UI-Fix in `ITDart.jsx`: besuchte, aber premium-gesperrte Modul-Themen sind jetzt optisch von nie angeklickten unterscheidbar. Dokument 29 zweimal weiter ausgebaut: neuer Abschnitt „Ablauf: Registrierung bis Löschung" (kompletter Nutzerlebenszyklus, inkl. Klarstellung „aktuell kein automatisierter Verkauf") sowie ein Urheberrechts-Block (eigene Inhalte/OpenArt-Lizenz/IHK-Abgrenzung) als bewusste Erweiterung über den ursprünglich engen Prüfauftrag hinaus. Neues Feature: Selbstbedienungs-Datenexport (Art. 20 DSGVO) — jeder Nutzer kann unter „Meine Statistik" eigenständig alle gespeicherten Daten als JSON+CSV herunterladen (`src/lib/dataExport.js`), kein Premium-Feature; damit der entsprechende offene Punkt in Dokument 29 sowie COMPLIANCE.md (jetzt 16/18) geschlossen. Subnetting-Übungswürfel (3D-Druck, Blender) als Nebenprojekt begonnen und pausiert (Würfel selbst fertig/wasserdicht in 3 Größen, Logo bewusst als separate Datei zum manuellen Ansetzen, siehe Claude-Gedächtnis). Sicherheits-Härtung auf Nutzerhinweis: Datenschutzerklärung Abschnitt 7 "Datensicherheit" um Transparenz zum Datenexport-Download ergänzt (keine Kopie/Kenntnisnahme bei uns) sowie um automatisierte Sicherheitsüberwachung erweitert. GitHub Dependabot Vulnerability Alerts + Automated Security Fixes aktiviert, `.github/dependabot.yml` für wöchentliche npm-/Actions-Update-PRs angelegt. Neue CSP-Verstoßmeldungen-Kette: `csp_reports`-Tabelle (RLS: nur Admin liest) + `csp-report` Edge Function (nimmt Browser-Reports ohne Auth entgegen, Ende-zu-Ende live getestet) + `report-to`/`report-uri` in `vercel.json` + Panel in `MonitoringScreen.jsx`. COMPLIANCE.md jetzt 18/20 (≈90%); doc 05 (Sicherheitskonzept) und doc 29 (Anwaltspaket) entsprechend nachgezogen (Abhängigkeits-Scanning jetzt "Umgesetzt", neue CSP-Zeile, Score-Referenz aktualisiert). WhatsApp/OG-Vorschaubild (`public/og-image.png`) neu gestaltet: Dartscheibe + selbst gezeichneter, bullseye-genau ausgerichteter Lernpfad statt des alten flachen Icons. Dabei Wettbewerbsrecht-Hinweis vom Nutzer aufgegriffen: "IHK-nah"/"IHK-Stil" aus allen rein werblichen Texten entfernt (Meta-Beschreibung `index.html`, OG-Bild, Impressum-Leistungsbeschreibung, Prüfungsbildschirm) und durch neutrales "im Format der Abschlussprüfung" ersetzt — faktische IHK-Nennungen in Lerninhalten blieben unverändert. Doc 29 entsprechend ergänzt.

**Zuletzt geändert (2026-08-02):** Musterantworten-Rollout endgültig abgeschlossen für alle 8 Module inkl. Datenbanken/Skripting/Beruf&Projekt/Karriere (vorher nur Grundlagen/Netzwerk/Sicherheit/Betriebssysteme). `IT-Dart-Generator/META_PROMPT.md` um 7 reale Lektionen ergänzt (Musterantworten-Pattern, Praxisfall-Qualitätskriterien, Modul-Reihenfolge nach Fachlogik, Quiz-Randomisierung inkl. Antwortoptionen, Favicon-Fallback, Versicherung als Kontrollpunkt, Trainer-Vereinbarung, AGENTS.md-Korrektur) und mit dem veröffentlichten Artefakt synchronisiert. Mehrere Claude-Code-Housekeeping-Funde: `.claude/settings.local.json` bereinigt (416→229 Einträge, u. a. ein eingebetteter Supabase-Secret-Key entfernt — Datei war korrekt gitignored, nie im Repo), zwei verwaiste Git-Worktrees entfernt, Troubleshooters verwaistes Gedächtnis-Verzeichnis (23 Dateien, lag unter dem alten Pfadnamen „IT-Dart-Klon") an den korrekten aktuellen Pfad migriert. To-Do #65 erneut durchleuchtet (15 IT-Dart-X-Konzeptkandidaten priorisiert — CCNA-Trainer und DSGVO-Schulung als stärkste echte Klon-Kandidaten; #1/#8 und #57/#58 als Duplikate markiert). E-Mail-Webserver-Check: `notify.it-dart.de` vollständig verifiziert (MX/DKIM/SPF korrekt, SPF liegt auf Resend-Bounce-Subdomain `send.notify.it-dart.de`), DMARC-Härtung (`p=none` → `quarantine` → `reject`) als neues To-Do angelegt. Neue EÜR-Tabellenvorlage (`IT-Dart Buchhaltung/EUER_IT-Dart.xlsx`) für die persönliche Einkommensangabe des Nutzers bei der Krankenkasse. Neues Dokument 30 (Notfall-Gesamtwiederherstellungsplan) erstellt: deckt PC-Ausfall, Claude-Code-Ausfall/Übergabe an Dritte, Supabase-/Vercel-/STRATO-/GitHub-/Resend-Ausfall, Hack-Sofortmaßnahmen, DSGVO-72h-Meldepflicht und ein Credential-Fundort-Inventar ab; Infrastrukturfakten (GitHub-Konto ist Einzelkonto kein Org, STRATO = Domain+DNS+SMTP, Resend = Benachrichtigungs-Mails separat) live gegen DNS/GitHub-API/Supabase-Projekt geprüft statt angenommen.

**Zuletzt geändert (2026-08-01):** Neue KI-Auswertung der Prüfungsvorbereitung (`mode:"auswertung"` in `ai-chat`) — kritische, datenbasierte Einschätzung anhand bisheriger 50-/150-Fragen-Lernzertifikate, sichtbar auf dem Prüfungs-Startbildschirm sobald ein qualifizierender Durchlauf existiert. Neues `KI-RICHTLINIEN.md` konsolidiert Datenschutz/Jugendschutz/AGG-Prinzipien für alle KI-Einsatzorte; Faktentreue-Prinzip jetzt auch in `todo-solve` verankert. Datenschutz um den dritten Anthropic-Anwendungsfall ergänzt. Modul-1-Inhalte präzisiert (Grafikkarte/Netzteil, TB-Einheit, RAM-Zusammenhang, BIOS-Tasten, DisplayPort, USB-A/USB-3.0-Klarstellung), Quiz-Reihenfolge jetzt pro Durchlauf randomisiert. Neue Trainer-Vereinbarung (Umfang/Zweckbindung/Vertraulichkeit des Trainee-Dateneinblicks) + AGB verweist jetzt an drei Stellen auf die passenden Datenschutz-Punkte statt zu duplizieren. Temporärer Wartungsmodus (`WARTUNGSMODUS` in `App.jsx`) für anonyme Besucher aktiv, bis Versicherung abgeschlossen ist (Vercel Pro ist erledigt). Modulreihenfolge an OSI-Relevanz angepasst (IT-Sicherheit direkt nach Netzwerktechnik). Fixierte KI-Musterantworten (a1/a2) jetzt über alle 8 Module fertig (46 von 48 Musterfragen; die 2 verbleibenden nutzen bewusst den interaktiven KI-Dialogmodus — Netzwerktechnik Thema 7, Karriere Thema 4). Modul-1-Praxisfälle über drei Nutzer-Feedbackrunden verfeinert, daraus eine 6-Punkte-Checkliste fürs weitere Content-Schreiben destilliert (Claude-Gedächtnis `feedback_praxisfall_content_checklist`) und rückwirkend auf Modul 4/5 angewendet. PNG-Favicon-Fallback ergänzt (Google-Suche unterstützt SVG-Favicons unzuverlässig). Neue Regel: Claude setzt To-Dos nie mehr selbst auf „done", nur noch „in_progress" + Notiz — Nutzer bestätigt im Dashboard.
